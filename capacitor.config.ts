import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'swipe-tabs-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
